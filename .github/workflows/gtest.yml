name: gtest

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: gtest-permission
      run: chmod a+x ./all.out 
    - name: gtest
      run: ./all.out 
